#!/usr/bin/openrc-run

description="Set up firewall with iptables."

depend()
{
	# after devfs
	keyword -docker -lxc -openvz -prefix -systemd-nspawn -uml -vserver -xenu
}

start()
{
    /home/cody/scr/firewall
}
